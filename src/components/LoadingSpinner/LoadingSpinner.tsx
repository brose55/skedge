import "./LoadingSpinner.css"

const LoadingSpinner = () => {
	return (
		<img
			src={"/icons/dark-gear.svg"}
			id="loading-spinner"
			alt="gear indicating page loading"
		/>
	)
}

export default LoadingSpinner
